{% extends '/all/user/profil_base.html.twig' %}

{% block main %}
    <h2>Your Orders</h2>
    <table class="table table-hover table-striped table-light">
        <thead>
        <tr>
            <th scope="col">Date</th>
            <th scope="col">Ref.</th>
            <th scope="col">Amount</th>
            <th scope="col">Description</th>
        </tr>
        </thead>
        <tbody>
            {% for o in orders %}
                <tr>
                    <th scope="row">{{ o.createdAt.format("d/m/y") }}</th>
                    <td> {{ o.idOrders }} </td>
                    <td> {{ o.totalPrice }} €</td>
                    <td>{{ o.description }}</td>
                </tr>
            {% endfor %}
        </tbody>
    </table>
{% endblock %}

{% block stylesheets %}
    <style>

    </style>
{% endblock %}

{% block javascripts %}

{% endblock %}
